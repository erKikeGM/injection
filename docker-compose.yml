services:
  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    environment:
      NODE_ENV: production
      PORT: "4173"
    ports:
      - "${APP_PORT:-4173}:4173"
    volumes:
      - ./platform/runs:/app/platform/runs
    restart: unless-stopped
